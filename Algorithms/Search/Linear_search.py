def search_linear(x,y):
  n = len( x )
  for i in range(n):

  if theValue[i] == y:
        return True

  return false

def main():
  mainValues =int( input("enter the  array values "))
  mV = mainValues.list()

  trgt =int( input('enter num to find')
   search_linear(mainValues, trgt)
