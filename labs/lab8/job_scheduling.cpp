#include <bits/stdc++.h>
using namespace std;

int main() {
	int t;
	scanf("%d",&t);
	while(t--) {
		int n;
		scanf("%d",&n);
		int d[n], p[n];

		for(int i = 0; i < n; i ++)
			scanf("%d ",&d[i]);

		for(int i = 0; i < n; i ++)
			scanf("%d ",&p[i]);
	}

	return 0;
}