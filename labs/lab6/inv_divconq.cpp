#include <bits/stdc++.h>

int inv(int arr[], int n) {
	int count;

	return count;
}

int main() {
	int n;
	scanf("%d",&n);

	int arr[n];
	for(int i = 0; i < n; i ++)
		scanf("%d",&arr[i]);

	int count = inv(arr, n);

	return 0;
}